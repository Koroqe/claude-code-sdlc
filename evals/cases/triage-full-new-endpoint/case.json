{
  "description": "A new API endpoint is a full-forcing signal (FR-1.3a) regardless of how small it looks.",
  "prompt": "Add a POST /api/subscribe endpoint that stores an email address.",
  "maxTurns": 6,
  "seed": {
    "src/server.js": "const routes = [];\nmodule.exports = { routes };\n"
  },
  "graders": [
    {
      "name": "protocol: stated a tier",
      "type": "regex",
      "pattern": "tier:\\s*(fast|quick|full)"
    },
    {
      "name": "correctness: tier is full",
      "type": "regex",
      "pattern": "tier:\\s*full"
    },
    {
      "name": "correctness: did not take a cheap tier",
      "type": "regex",
      "pattern": "tier:\\s*(fast|quick)",
      "match": "not_contains"
    }
  ],
  "_note": "maxTurns=6 deliberately: measured 2026-08-24, maxTurns=2 starved two cases into a FALSE failure (the tier was never stated because the run was cut off, not because Triage misfired). A too-tight turn budget manufactures defects that do not exist."
}