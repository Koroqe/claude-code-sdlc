# Architecture Rules

## Project Structure

TODO: Define your project's module boundaries and file responsibilities:
- Where do API endpoints go?
- Where does business logic live?
- Where is the data access layer?
- Where are external integrations?

## Hard Constraints

TODO: Define inviolable rules:
- Which modules must never import from each other?
- What patterns are required for new endpoints?
- What validation approach must be used?
