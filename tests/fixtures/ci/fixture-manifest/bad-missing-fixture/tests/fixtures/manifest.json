{
  "_purpose": "Seeded-bad mirror of tests/fixtures/manifest.json for scripts/ci/validate-fixture-manifest.js's falsify step. One entry, whose fixture path deliberately does not exist relative to this seeded root. `qaDoc` was added (Slice 11, generalized validator) so this single-document root still fails for ITS OWN original reason — the rotted fixture path — rather than for the new required-`qaDoc`/wholesale-unregistered checks the generalized validator also performs; run with `--min 1` since this root deliberately discovers only one document, below the real tree's 10-document floor.",
  "cases": [
    {
      "id": "TC-1.1",
      "agent": "verifier",
      "qaDoc": "docs/qa/verification-review-upgrade_test_cases.md",
      "fixture": "tests/fixtures/agents/verifier/present-unverified/",
      "expect": "verdict PRESENT_BEHAVIOR_UNVERIFIED for a fully wired but never-exercised route"
    }
  ]
}
