---
name: verifier
description: Goal-backward integration verification — seeded stand-in for the falsify fixture, present only so the "agent exists" assertion passes cleanly and the failure isolates to the cross-document mismatch.
tools: Read, Glob, Grep, Write
model: sonnet
---

# Verifier (seeded stand-in)

Placeholder content. This file exists only so
`scripts/ci/validate-fixture-manifest.js`'s "agent exists as agents/<name>.md" check passes for this
seeded fixture — it is not used for anything else.
