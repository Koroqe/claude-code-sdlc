# Seeded Bad Agent Fixture — No Frontmatter

This file deliberately opens with a heading instead of a `---` fence, so
`validate-agents.js` must report that the frontmatter is missing.

It is never installed and never shipped.
