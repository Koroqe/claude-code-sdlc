Delegate to `planner` for the current full-tier feature.

Current feature: **PRD Section 11 — Self-Improvement Loop**.

Read `.claude/instincts.md`'s `## Prevention Rules` before producing the plan (existence-guarded — an
absent file is a designed state, not an error). Consult `docs/digest-index.md` for prior-feature
context and pull only the sections that bear on this one.
