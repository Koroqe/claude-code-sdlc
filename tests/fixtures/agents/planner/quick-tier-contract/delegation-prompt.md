Invoke `planner` under its Quick-Tier Contract mode.

Feature/fix description (the ONLY input supplied — no PRD section, no use-cases file, no QA file,
no architecture review):

  The password-reset email links expire after 15 minutes, but the copy in the email says 1 hour.
  Make the copy match the actual expiry.
