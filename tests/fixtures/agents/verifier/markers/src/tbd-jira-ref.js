'use strict';

/**
 * TC-4.13: TBD with a project-key issue reference (`<UPPERCASE>-<digits>`)
 * on the same line — expected: WARNING (downgraded), Level 2 still PASSES.
 */
function computeTaxRate(region) {
  // TBD JIRA-456
  return 0;
}

module.exports = { computeTaxRate };
