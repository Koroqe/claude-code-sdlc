'use strict';

/**
 * TC-4.6: PLACEHOLDER (uppercase) — expected: WARNING (unconditional),
 * Level 2 still PASSES.
 */
const DEFAULT_AVATAR_URL = 'https://example.com/avatar-PLACEHOLDER.png';

module.exports = { DEFAULT_AVATAR_URL };
