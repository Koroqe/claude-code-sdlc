'use strict';

// TC-4.1: bare TBD, no issue reference on this line — expected: BLOCKER.
function computeDiscount(order) {
  // TBD
  return order.total * 0.9;
}

module.exports = { computeDiscount };
