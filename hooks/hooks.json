{
  "_comment": [
    "Hook configuration for the claude-code-sdlc plugin.",
    "",
    "Claude Code loads this automatically when the plugin is installed.",
    "NEVER copy its contents into settings.json — plugin hooks auto-load, and",
    "duplicating them there makes every hook fire twice.",
    "",
    "Every handler is invoked through hooks/lib/run-hook.js. There is no inline",
    "bootstrap in any command string: the wrapper is where the fail-open",
    "contract, the Node version gate and the runtime kill switches live, so a",
    "hook that bypassed it would bypass all of them. CI enforces this.",
    "",
    "No hook here blocks. Every one exits 0 whatever happens; blocking guards",
    "arrive in PRD Section 8.",
    "",
    "Runtime controls: SDLC_HOOKS_ENABLED=0 disables everything;",
    "SDLC_DISABLED_HOOKS is a comma-separated id list; SDLC_HOOK_PROFILE is",
    "minimal | standard | strict (invalid values fall back to standard)."
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "id": "session:start:spine",
            "type": "command",
            "description": "Injects current feature, branch, wave and slice from the scratchpad so a resumed or compacted session re-enters the autonomous loop at the right point. Also reports memory-layer version drift.",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/lib/run-hook.js\" --hook session:start:spine",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "id": "post:edit:accumulate",
            "type": "command",
            "description": "Records each edited path so the Stop hook can run format and typecheck once per response instead of once per edit.",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/lib/run-hook.js\" --hook post:edit:accumulate",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "id": "stop:typecheck-format",
            "type": "command",
            "description": "Runs the project's declared format and typecheck commands once over everything edited this response. No-ops visibly when the project declares no such command.",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/lib/run-hook.js\" --hook stop:typecheck-format",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
